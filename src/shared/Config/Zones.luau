--!strict
-- Depth zones: geometry (studs), displayed depth (metres), lighting, pools and hazards.
--
-- The world is one vertical shaft centred on (0, y, 0). Surface is y = 0 and y goes negative
-- downward. Each zone occupies [bottomY, topY]; its floor slab fills the lowest
-- `floorThickness` studs, with a hole (offset from the centre) leading to the next zone.
-- Displayed metres map linearly from the zone's stud band onto `meters`.

export type ZoneLighting = {
	fogColor: Color3,
	fogStart: number,
	fogEnd: number, -- ignored when dark = true (light radius decides)
	ambient: Color3,
	outdoorAmbient: Color3,
	brightness: number,
	tint: Color3,
	saturation: number,
	contrast: number,
	ccBrightness: number,
	bloom: number,
}

export type Zone = {
	id: string,
	index: number,
	name: string,
	short: string,
	desc: string,
	hazard: string,
	color: Color3,
	topY: number,
	bottomY: number,
	radius: number,
	floorThickness: number,
	hole: { x: number, z: number, r: number }?,
	terraces: { { r: number, y: number } }?,
	meters: { number },
	materials: { floor: Enum.Material, wall: Enum.Material, accent: Enum.Material },
	materialColors: { floor: Color3, wall: Color3, accent: Color3 },
	lighting: ZoneLighting,
	dark: boolean,
	oxygenDrain: number, -- oxygen units per second
	rarityBonus: number, -- multiplies the chance of Uncommon+ tiers
	valueScale: number, -- scales event-exclusive item values and quest/daily rewards
	fishDensity: number, -- target fish within Game.Spawns.DensityRadius of each diver
	treasureDensity: number, -- target treasure within Game.Spawns.TreasureRadius of each diver
	predatorDensity: number,
	predators: { string },
	requiresRebirth: boolean,
	ambient: string, -- Sounds key
	music: string, -- Sounds key
	decor: { [string]: number },
}

local list: { Zone } = {
	{
		id = "Shallows",
		index = 1,
		name = "Sunlit Shallows",
		short = "Shallows",
		desc = "Warm, bright reef flats around the rim of the Blue Hole. Safe waters.",
		hazard = "Safe waters",
		color = Color3.fromRGB(90, 220, 255),
		topY = 0,
		bottomY = -300,
		radius = 460,
		floorThickness = 60,
		hole = { x = 0, z = 0, r = 150 },
		terraces = {
			{ r = 460, y = -70 },
			{ r = 380, y = -120 },
			{ r = 300, y = -180 },
			{ r = 230, y = -240 },
		},
		meters = { 0, 200 },
		materials = { floor = Enum.Material.Sand, wall = Enum.Material.Limestone, accent = Enum.Material.Sandstone },
		materialColors = {
			floor = Color3.fromRGB(232, 212, 160),
			wall = Color3.fromRGB(205, 195, 165),
			accent = Color3.fromRGB(196, 160, 120),
		},
		lighting = {
			fogColor = Color3.fromRGB(38, 150, 190),
			fogStart = 20,
			fogEnd = 430,
			ambient = Color3.fromRGB(95, 145, 165),
			outdoorAmbient = Color3.fromRGB(125, 175, 195),
			brightness = 2.0,
			tint = Color3.fromRGB(215, 245, 255),
			saturation = 0.15,
			contrast = 0.05,
			ccBrightness = 0.02,
			bloom = 0.6,
		},
		dark = false,
		oxygenDrain = 1.0,
		rarityBonus = 1.0,
		valueScale = 1,
		fishDensity = 18,
		treasureDensity = 7,
		predatorDensity = 0,
		predators = {},
		requiresRebirth = false,
		ambient = "AmbientShallows",
		music = "MusicShallows",
		decor = { kelp = 70, coral = 110, lightShafts = 14, rocks = 40, anemones = 40, shells = 30 },
	},
	{
		id = "Twilight",
		index = 2,
		name = "Twilight Reef",
		short = "Twilight",
		desc = "Dimming light over steep reef ledges. The first predators hunt here.",
		hazard = "Predators",
		color = Color3.fromRGB(70, 120, 255),
		topY = -300,
		bottomY = -700,
		radius = 380,
		floorThickness = 60,
		hole = { x = 110, z = -60, r = 130 },
		terraces = nil,
		meters = { 200, 1000 },
		materials = { floor = Enum.Material.Ground, wall = Enum.Material.Rock, accent = Enum.Material.Sandstone },
		materialColors = {
			floor = Color3.fromRGB(96, 102, 118),
			wall = Color3.fromRGB(82, 92, 116),
			accent = Color3.fromRGB(196, 160, 120),
		},
		lighting = {
			fogColor = Color3.fromRGB(16, 58, 110),
			fogStart = 10,
			fogEnd = 270,
			ambient = Color3.fromRGB(52, 82, 124),
			outdoorAmbient = Color3.fromRGB(52, 82, 124),
			brightness = 1.2,
			tint = Color3.fromRGB(190, 215, 255),
			saturation = 0.05,
			contrast = 0.06,
			ccBrightness = 0,
			bloom = 0.8,
		},
		dark = false,
		oxygenDrain = 1.25,
		rarityBonus = 1.3,
		valueScale = 4,
		fishDensity = 17,
		treasureDensity = 7,
		predatorDensity = 1,
		predators = { "ReefBarracuda" },
		requiresRebirth = false,
		ambient = "AmbientTwilight",
		music = "MusicTwilight",
		decor = { kelp = 30, coral = 80, rocks = 50, ledges = 14, anemones = 30, arches = 4 },
	},
	{
		id = "Midnight",
		index = 3,
		name = "Midnight Zone",
		short = "Midnight",
		desc = "No sunlight reaches this deep. Your lights are all you have, but some fish glow.",
		hazard = "Darkness",
		color = Color3.fromRGB(150, 90, 255),
		topY = -700,
		bottomY = -1100,
		radius = 360,
		floorThickness = 60,
		hole = { x = -120, z = 90, r = 120 },
		terraces = nil,
		meters = { 1000, 3000 },
		materials = { floor = Enum.Material.Slate, wall = Enum.Material.Slate, accent = Enum.Material.Glacier },
		materialColors = {
			floor = Color3.fromRGB(44, 48, 66),
			wall = Color3.fromRGB(38, 42, 60),
			accent = Color3.fromRGB(90, 70, 160),
		},
		lighting = {
			fogColor = Color3.fromRGB(4, 10, 24),
			fogStart = 0,
			fogEnd = 120,
			ambient = Color3.fromRGB(26, 32, 56),
			outdoorAmbient = Color3.fromRGB(10, 14, 30),
			brightness = 0.4,
			tint = Color3.fromRGB(175, 190, 255),
			saturation = -0.05,
			contrast = 0.08,
			ccBrightness = 0,
			bloom = 1.2,
		},
		dark = true,
		oxygenDrain = 1.5,
		rarityBonus = 1.7,
		valueScale = 15,
		fishDensity = 15,
		treasureDensity = 6,
		predatorDensity = 2,
		predators = { "GulperEel" },
		requiresRebirth = false,
		ambient = "AmbientMidnight",
		music = "MusicMidnight",
		decor = { glowCoral = 90, rocks = 50, ledges = 12, vents = 6, jellies = 20 },
	},
	{
		id = "Abyssal",
		index = 4,
		name = "Abyssal Plains",
		short = "Abyssal",
		desc = "A vast silt plain littered with the wrecks of a hundred ships. Heavy pressure.",
		hazard = "Crushing pressure",
		color = Color3.fromRGB(60, 200, 190),
		topY = -1100,
		bottomY = -1500,
		radius = 480,
		floorThickness = 60,
		hole = { x = 40, z = 90, r = 100 },
		terraces = nil,
		meters = { 3000, 6000 },
		materials = { floor = Enum.Material.Mud, wall = Enum.Material.Concrete, accent = Enum.Material.Salt },
		materialColors = {
			floor = Color3.fromRGB(62, 60, 58),
			wall = Color3.fromRGB(48, 50, 56),
			accent = Color3.fromRGB(120, 130, 125),
		},
		lighting = {
			fogColor = Color3.fromRGB(6, 9, 14),
			fogStart = 0,
			fogEnd = 110,
			ambient = Color3.fromRGB(22, 24, 34),
			outdoorAmbient = Color3.fromRGB(8, 10, 16),
			brightness = 0.3,
			tint = Color3.fromRGB(185, 195, 220),
			saturation = -0.15,
			contrast = 0.1,
			ccBrightness = 0,
			bloom = 1.1,
		},
		dark = true,
		oxygenDrain = 1.8,
		rarityBonus = 2.2,
		valueScale = 60,
		fishDensity = 15,
		treasureDensity = 8,
		predatorDensity = 2,
		predators = { "AbyssalShark" },
		requiresRebirth = false,
		ambient = "AmbientAbyssal",
		music = "MusicAbyssal",
		decor = { wrecks = 9, rocks = 60, bones = 5, vents = 5, ledges = 8 },
	},
	{
		id = "Hadal",
		index = 5,
		name = "Hadal Trench",
		short = "Hadal",
		desc = "A narrow trench under extreme pressure. Ancient ruins and enormous creatures.",
		hazard = "Extreme pressure",
		color = Color3.fromRGB(255, 120, 70),
		topY = -1500,
		bottomY = -1900,
		radius = 260,
		floorThickness = 60,
		hole = { x = -30, z = 20, r = 70 },
		terraces = nil,
		meters = { 6000, 11000 },
		materials = { floor = Enum.Material.Asphalt, wall = Enum.Material.Basalt, accent = Enum.Material.Cobblestone },
		materialColors = {
			floor = Color3.fromRGB(30, 30, 34),
			wall = Color3.fromRGB(34, 32, 36),
			accent = Color3.fromRGB(84, 78, 70),
		},
		lighting = {
			fogColor = Color3.fromRGB(4, 4, 8),
			fogStart = 0,
			fogEnd = 100,
			ambient = Color3.fromRGB(18, 17, 24),
			outdoorAmbient = Color3.fromRGB(6, 6, 10),
			brightness = 0.2,
			tint = Color3.fromRGB(205, 192, 190),
			saturation = -0.2,
			contrast = 0.12,
			ccBrightness = 0,
			bloom = 1.3,
		},
		dark = true,
		oxygenDrain = 2.2,
		rarityBonus = 3.0,
		valueScale = 240,
		fishDensity = 13,
		treasureDensity = 8,
		predatorDensity = 3,
		predators = { "TrenchKraken" },
		requiresRebirth = false,
		ambient = "AmbientHadal",
		music = "MusicHadal",
		decor = { ruins = 8, pillars = 30, rocks = 40, vents = 6, giants = 2 },
	},
	{
		id = "Rift",
		index = 6,
		name = "The Rift",
		short = "Rift",
		desc = "Something alien glows beneath the trench. Only the Resurfaced may pass the Seal.",
		hazard = "Unknown",
		color = Color3.fromRGB(230, 90, 255),
		topY = -1900,
		bottomY = -2300,
		radius = 420,
		floorThickness = 80,
		hole = nil,
		terraces = nil,
		meters = { 11000, 14000 },
		materials = { floor = Enum.Material.Glacier, wall = Enum.Material.Ice, accent = Enum.Material.Salt },
		materialColors = {
			floor = Color3.fromRGB(110, 70, 210),
			wall = Color3.fromRGB(60, 32, 110),
			accent = Color3.fromRGB(200, 120, 255),
		},
		lighting = {
			fogColor = Color3.fromRGB(34, 8, 54),
			fogStart = 10,
			fogEnd = 320,
			ambient = Color3.fromRGB(86, 44, 128),
			outdoorAmbient = Color3.fromRGB(60, 22, 100),
			brightness = 0.8,
			tint = Color3.fromRGB(232, 205, 255),
			saturation = 0.35,
			contrast = 0.1,
			ccBrightness = 0.02,
			bloom = 1.6,
		},
		dark = false,
		oxygenDrain = 2.6,
		rarityBonus = 4.0,
		valueScale = 1000,
		fishDensity = 14,
		treasureDensity = 9,
		predatorDensity = 3,
		predators = { "RiftStalker" },
		requiresRebirth = true,
		ambient = "AmbientRift",
		music = "MusicRift",
		decor = { crystals = 110, floaters = 24, rocks = 30, spires = 10 },
	},
}

local byId: { [string]: Zone } = {}
for _, zone in list do
	byId[zone.id] = zone
end

-- Surface lighting (above water at the dock)
local surface = {
	ambient = Color3.fromRGB(110, 115, 125),
	outdoorAmbient = Color3.fromRGB(140, 150, 160),
	brightness = 2.4,
	clockTime = 13.5,
	fogColor = Color3.fromRGB(170, 210, 235),
	fogStart = 350,
	fogEnd = 2300,
	tint = Color3.fromRGB(255, 255, 255),
	saturation = 0.1,
	contrast = 0.05,
	ccBrightness = 0,
	bloom = 0.4,
}

-- Dock island / atoll geometry (studs)
local world = {
	atollOuterRadius = 560, -- the atoll ring spans Shallows.radius … atollOuterRadius
	atollTopY = 5,
	waterDepth = 8, -- thin terrain water layer at the surface
	ceilingThickness = 30, -- rock ceiling over a zone wider than the one above it
	wallThickness = 60,
	dockCenter = Vector3.new(0, 5, -660), -- centre of the dock island
	dockRadius = 200,
	plazaCenter = Vector3.new(0, 5, -565),
	pierStart = Vector3.new(0, 6, -470), -- where you arrive back on the dock after a dive
	pierEnd = Vector3.new(0, 6, -398),
	launchPoint = Vector3.new(0, -14, -380), -- centre of the dive area just below the surface
	launchHang = Vector3.new(0, 12.5, -380), -- where the sub hangs from the Launch Bay crane
	launchLook = Vector3.new(0, -30, -250),
	characterBarrierY = -7,
	boundaryHalfX = 700, -- invisible character walls around the whole playable area
	boundaryMinZ = -900,
	boundaryMaxZ = 600,
	-- The Rift Seal sits in the Hadal floor hole. Players without a rebirth are pushed back above it.
	riftSealY = -1845,
}

return {
	list = list,
	byId = byId,
	count = #list,
	surface = surface,
	world = world,
	surfaceY = 0,
	bottomY = list[#list].bottomY,
}
